# 本文件只用于本地开发，通过 Next.js 开发服务器提供前端热更新，并使用国内 npm 镜像安装依赖。
FROM node:22-alpine

WORKDIR /app
RUN npm config set registry https://registry.npmmirror.com
COPY package.json package-lock.json ./
RUN npm ci
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--hostname", "0.0.0.0", "--port", "3000"]
