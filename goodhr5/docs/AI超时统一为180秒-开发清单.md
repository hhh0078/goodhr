<!-- 本文件用于记录云端与 Local Agent 的 AI 请求超时统一工作及验证结果。 -->

# AI 超时统一为 180 秒开发清单

- [x] 排查云端 AI 中转、配置测试、岗位要求优化和帮助助手超时。
- [x] 排查 Local Agent AI HTTP 客户端及云端配置默认值。
- [x] 排查基础预评分、详情评分、最终评分和候选人总处理超时。
- [x] 将云端所有 AI HTTP 请求统一为 180 秒。
- [x] 将 Local Agent 所有 AI HTTP 请求默认值统一为 180 秒。
- [x] 将岗位运行 AI 操作及候选人总处理上限统一为 180 秒。
- [x] 增加云端、本地 AI 客户端和岗位运行超时回归测试。
- [x] 运行 Go 测试与格式检查。
- [x] 提交功能分支、合并主分支并推送。

## 验证结果

- 云端后端 `go test ./...` 通过。
- Local Agent `go test ./internal/localai ./internal/positionrunner` 通过。
- Local Agent 全量测试仍有两项与本次修改无关的既有环境失败：Windows SID 导致默认浏览器配置未生成、Node Runtime 测试资源版本不一致。
