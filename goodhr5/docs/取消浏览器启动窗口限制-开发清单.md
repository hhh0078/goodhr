<!-- 本文件用于记录测试取消浏览器启动窗口尺寸与缩放限制的修改和验证结果。 -->

# 取消浏览器启动窗口限制开发清单

- [x] 定位 Node Worker 主入口与通用浏览器操作入口。
- [x] 注释固定 `1440×900` 视口和 `--window-size` 启动参数。
- [x] 使用 `viewport: null` 让内容视口跟随真实浏览器窗口。
- [x] 注释启动和复用时的100%缩放重置、固定视口设置及尺寸拦截。
- [x] 注释 Local Agent 和岗位运行向 Worker 注入固定视口参数的代码。
- [x] 注释备用 Go 浏览器入口的 `--window-size` 参数。
- [x] 保留原固定窗口代码，方便测试完成后恢复。
- [x] 增加浏览器启动参数不含固定窗口尺寸的回归测试。
- [x] 运行 Node 与 Go 相关测试。
- [x] 提交功能分支、合并主分支并推送。
