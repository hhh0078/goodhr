<!-- 文件作用说明：记录 GoodHR 自动回复的分支策略、阶段任务、测试标准和实际完成进度。 -->

# 自动回复开发步骤

## 1. 分支规则

- 大分支：`codex/auto-reply`，从最新 `main` 创建。
- 每个阶段建立一个 `codex/auto-reply-*` 小分支。
- 小分支完成后测试、中文提交、推送，再合并回 `codex/auto-reply`。
- 自动回复大分支暂时不合并 `main`。
- 每次合并后更新本文状态、提交号和验证结果。

## 2. 总体验收目标

岗位页可以配置并开启自动回复。猎聘企业端收到候选人新消息后，本地程序可以唯一识别岗位、差量同步聊天、补齐简历、调用支持工具的 AI、维护候选人确认项并安全发送回复；无法处理时只通知 HR，不暂停后续判断。主动打招呼运行时可以在每位候选人前插入最多3个自动回复会话，两个主流程代码保持独立。

## 3. 阶段清单

### 阶段0：方案和长期文档

分支：`codex/auto-reply-docs`

- [x] 核对现有自动回复骨架和旧文档。
- [x] 确认产品规则。
- [x] 建立自动回复思维文档。
- [x] 建立本开发步骤文档。
- [x] 修订本地 Agent 数据边界和文档入口。
- [x] 提交、推送并合并到大分支。

验证：Markdown 结构、相对链接、分支名和规范一致性。

### 阶段1：云端数据模型和存储

分支：`codex/auto-reply-data`

- [x] 团队多公司档案表和权限。
- [x] 岗位自动回复配置、开关、描述和默认索要简历话术。
- [x] 岗位条件表，支持必须满足、需要确认、加分项。
- [x] 临时会话、平台身份和岗位归属表。
- [x] 聊天消息差量表和幂等约束。
- [x] 简历附件元数据和手机号合并规则。
- [x] 候选人确认项和变更证据。
- [x] AI请求、回复、工具调用、工具结果和配置建议审计。
- [x] 180天审计与人工接管邮件幂等记录清理能力。

验证：Go 单元测试、存储测试、干净 PostgreSQL 迁移、重复迁移。

### 阶段2：云端接口、权限和通知

分支：`codex/auto-reply-cloud-api`

- [x] 公司档案和岗位配置接口。
- [x] 自动回复开关、Max/体验会员检测和状态接口。
- [x] 本地 Agent 会话解析、消息同步、简历上传和确认项接口。
- [x] AI审计写入和分页查询接口。
- [x] 岗位或候选人无法确定时的通知接口。
- [x] 邮件包含岗位、候选人、性别、平台和原因。
- [x] 团队、岗位、账号、机器绑定和文件校验。

验证：接口、越权、20MB文件边界、扩展名和邮件去重测试。

### 阶段3：岗位页、公司档案和审计界面

分支：`codex/auto-reply-frontend`

- [ ] 岗位卡片自动回复开关和编辑入口。
- [ ] 岗位描述、公司档案和默认索要简历话术。
- [ ] 多条件编辑、类型选择、排序和去重提示。
- [ ] 团队公司档案管理。
- [ ] 自动回复审计详情。
- [ ] Max权限、错误确认框和运行状态同步。

验证：TypeScript 检查、Next.js 构建、空状态和错误状态。

### 阶段4：本地云端客户端和强类型模型

分支：`codex/auto-reply-local-contracts`

- [ ] 岗位自动回复快照和实时开关读取。
- [ ] 会话、消息、简历、附件、确认项和审计强类型。
- [ ] 云端读写客户端和稳定错误码。
- [ ] 20MB附件上传和哈希。
- [ ] 本地隐私日志摘要。

验证：Go 单元测试、HTTP协议、取消和超时测试。

### 阶段5：公共自动回复主流程

分支：`codex/auto-reply-local-flow`

- [ ] 重构 `internal/flow/auto_reply`，保持平铺编排。
- [ ] 当前 URL 平台识别。
- [ ] 未读入口、会话快照和单轮最多3人。
- [ ] 首次最多5000条、后续差量同步。
- [ ] 岗位归属、候选人身份和手机号后备匹配。
- [ ] 简历索要、附件检查、下载监听和正式入库门槛。
- [ ] 发送前复核、发送后回读和幂等保护。
- [ ] 优雅停止和三次连续错误策略。

验证：流程单元测试、伪平台集成测试、停止和并发测试。

### 阶段6：AI工具、确认项和审计

分支：`codex/auto-reply-ai-tools`

- [ ] 标准 OpenAI `tool_calls` 流式解析和能力检测。
- [ ] 九个本地工具及强类型参数校验。
- [ ] 最多8次工具调用、最多2次参数修正。
- [ ] 固定 system 与动态 user/tool 消息分离。
- [ ] 确认项自动去重和证据更新。
- [ ] 岗位、公司修改建议，不允许 AI 直接修改。
- [ ] 一条候选人新消息最多发送一条回复。
- [ ] 总记录同步到运行小窗和云端审计。

验证：SSE/tool_calls、错误修正、重复发送和提示词缓存边界测试。

### 阶段7：浏览器调度和小窗状态

分支：`codex/auto-reply-scheduler-status`

- [ ] 主动打招呼每位候选人前读取自动回复开关。
- [ ] 外层调度器在两个独立流程之间切换。
- [ ] 同一浏览器只有一个页面动作执行者。
- [ ] 关闭开关后处理完当前会话再停止。
- [ ] 小窗显示 AI 等待、回复、工具、结果和总记录。
- [ ] 普通页面日志不立即覆盖 AI 结果。

验证：调度顺序、开关实时性和运行小窗构建测试。

### 阶段8：猎聘企业端页面实现

分支：`codex/auto-reply-liepin`

- [ ] 用现有 CloakBrowser 实测消息入口和未读数字。
- [ ] 实测联系人侧边栏、当前会话身份和沟通岗位位置。
- [ ] 实测平台候选人 ID。
- [ ] 实测聊天历史滚动和消息类型。
- [ ] 实测简历卡片、在线简历和附件下载。
- [ ] 补齐 `liepin/config.json` 选择器。
- [ ] 只在 `liepin/reply.go` 组织平台动作。
- [ ] 使用现有猎聘企业岗位完成真实自动回复。

验证：至少一条真实候选人消息完整闭环，并检查云端数据、邮件、AI审计和小窗。

### 阶段9：完整回归和交付检查

分支：`codex/auto-reply-integration`

- [ ] 云端 Go 测试。
- [ ] 本地 Go Agent 测试。
- [ ] Worker TypeScript 检查和测试。
- [ ] Next.js 构建。
- [ ] PostgreSQL 干净迁移和重复迁移。
- [ ] macOS 本地打包检查。
- [ ] `git diff --check`。
- [ ] 更新文档、已知风险和最终验证记录。
- [ ] 推送大分支，仍不合并 `main`。

## 4. 完成记录

| 阶段 | 分支 | 提交 | 合并到大分支 | 验证摘要 |
| --- | --- | --- | --- | --- |
| 0 | `codex/auto-reply-docs` | `1ba87569` | 已完成 | `git diff --check`、权威文档与数据边界核对通过 |
| 1 | `codex/auto-reply-data` | `2c341fde` | 已完成（`c19c2e1c`） | 云端 `go test ./...`、干净 PostgreSQL 完整存储链路、迁移重复执行和字段中文备注检查通过 |
| 2 | `codex/auto-reply-cloud-api` | `b2edf0bb` | 已完成（`2bf7100c`） | 云端 `go test ./...`、`go vet ./...`、真实与干净 PostgreSQL HTTP/存储链路连续两次通过，四份 Compose 配置和 `git diff --check` 通过 |
